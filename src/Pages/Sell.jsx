import { Typography } from "antd";
import React from "react";
import CustomLayout from "../components/Layout";
const Sell = () => {
    return (
        <CustomLayout>
            <Typography.Title level={2}>Sell</Typography.Title>
        </CustomLayout>
    );
};

export default Sell;
